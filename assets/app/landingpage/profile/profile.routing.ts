/*
import { Routes, RouterModule } from "@angular/router";

import { UserImagesComponent } from "./userimages.component";
import { FriendsComponent } from "./friends.component";

const PROFILE_ROUTES: Routes = [
    { path: '', redirectTo: 'photos', pathMatch: 'full' },
    { path: 'photos', component: UserImagesComponent, outlet: 'photo' },
    { path: 'friends', component: FriendsComponent, outlet: 'friend' }
];

export const profile_routing = RouterModule.forChild(PROFILE_ROUTES);
*/
